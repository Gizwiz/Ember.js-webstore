<div class="container-fluid item_display_wrapper">
    <div class="row">
        <div class="col"></div>
        <div class="col"></div>
        <div class="col">{{list-filter options='{"options":["Popularity", "Price: (Low - High)", "Price: (High - Low)","Name: (A - Z)", "Name: (Z - A)"]}' sortAction=this.sortAction}}</div>
    </div>
    {{#each this.filteredBikes as |bike index|}}
        <div class="row"> 
            <div class="col-md-3 item_preview_iamge"> 
                    <img src="{{bike.img}}" alt="" class="item_preview_image" >
            </div>
            <div class="col-md-7">
                    <div class="row">
                        {{#link-to 'item' (query-params id=bike.id)}}<span>{{bike.name}}</span>{{/link-to}}
                    </div>
 
                    <div class="row">
                        <span>{{bike.description}}</span>
                    </div>
            </div>
            <div class="col-md-2">
                <div class="row">
                    <ul>
                        {{#each bike.salesPoints as |point|}}
                            <li>{{point}}</li>
                        {{/each}}
                    </ul>
                </div>
                <div class="row">
                    <span class="price">{{bike.price}}{{bike.unit}}</span>
                </div>
                <div class="row">
                    <div class="row-actions">
                        <button>View Item ></button>
                    </div>
                </div>

            </div>
                    <!-- <div class="row">
                            <div class="container">
                                    <div class="row item_preview_first_row">
                                        <img src="{{bike.img}}" alt="" class="item_preview_image" >
                                        <ul>
                                            {{#each bike.salesPoints as |point|}}
                                                <li>{{point}}</li>
                                            {{/each}}
                                        </ul>
                                    </div>
                                    <div class="row">
                                        {{log bike.id}}
                                        {{#link-to 'item' (query-params id=bike._id)}}<span>{{bike.name}}&nbsp;{{bike._id}}</span>{{/link-to}}
                                    </div>
                                    <div class="row">
                                        <span>{{bike.price}}{{bike.unit}}</span>
                                    </div>
                                    <div class="row">
                                        <span>{{bike.description}}</span>
                                    </div>
                            </div>
                    </div> -->
        </div>
    {{/each}}
</div>
<script>
   // $(".page_wrapper").fadeIn(300);
</script>